# folder-files-renamer
This python script takes in the path of a folder and a string to remove. After executing the script, that string will be removed from name of all files of that folder (directory).
